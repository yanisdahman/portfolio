name: Fetch RSS Feeds (disabled)

on:
  workflow_dispatch:

jobs:
  fetch:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Disabled
        run: echo "Ce workflow est désactivé. Les articles sont désormais statiques."
